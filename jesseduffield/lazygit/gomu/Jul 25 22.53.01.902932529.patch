--- a/playlist.go
+++ b/playlist.go
@@ -547,6 +547,7 @@ Download:
 		case <-time.After(time.Millisecond * 100):
 
 			r, g, b := gomu.accentColor.RGB()
+			hexColor := padHex(r, g, b)
 			p.SetTitle(title)
 			gomu.app.Draw()
 		}
