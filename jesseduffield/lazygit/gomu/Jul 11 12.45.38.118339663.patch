--- a/player.go
+++ b/player.go
@@ -140,9 +140,8 @@ next:
 			p.format = nil
 			gomu.PlayingBar.Stop()
 
-			if gomu.Queue.GetItemCount() != 0 {
 				go p.Run()
 			}
 			break next
 
 		case <-time.After(time.Second):
