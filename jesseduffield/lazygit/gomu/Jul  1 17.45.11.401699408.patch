--- a/playlist.go
+++ b/playlist.go
@@ -280,17 +280,16 @@ func populate(root *tview.TreeNode, rootPath string) {
 				log(err.Error())
 			}
 
-			Name:        file.Name(),
 			Path:        path,
 			IsAudioFile: true,
 			Parent:      root,
 		}
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
