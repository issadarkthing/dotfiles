--- a/playlist.go
+++ b/playlist.go
@@ -291,9 +291,10 @@ func populate(root *tview.TreeNode, rootPath string) {
 			child.SetReference(audioFile)
 
 		}
+
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
