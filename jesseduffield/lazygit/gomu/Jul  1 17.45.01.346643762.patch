--- a/playlist.go
+++ b/playlist.go
@@ -258,6 +258,7 @@ func populate(root *tview.TreeNode, rootPath string) {
 
 		defer f.Close()
 
+		child := tview.NewTreeNode(file.Name())
 		if !file.IsDir() {
 
 			filetype, err := GetFileContentType(f)
