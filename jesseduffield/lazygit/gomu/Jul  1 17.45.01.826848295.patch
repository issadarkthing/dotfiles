--- a/playlist.go
+++ b/playlist.go
@@ -259,6 +259,7 @@ func populate(root *tview.TreeNode, rootPath string) {
 		defer f.Close()
 
 		child := tview.NewTreeNode(file.Name())
+		root.AddChild(child)
 		if !file.IsDir() {
 
 			filetype, err := GetFileContentType(f)
