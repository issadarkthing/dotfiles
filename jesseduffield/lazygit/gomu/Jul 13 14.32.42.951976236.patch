--- a/playlist.go
+++ b/playlist.go
@@ -382,7 +382,6 @@ func (p *Playlist) Refresh() {
 
 		// to preserve previously highlighted node
 		if node.GetReference().(*AudioFile).Name == prevFileName {
-		if node.GetText() == prevFileName {
 			p.SetHighlight(node)
 			return false
 		}
