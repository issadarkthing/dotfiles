--- a/playlist.go
+++ b/playlist.go
@@ -228,6 +228,7 @@ func (p *Playlist) deleteSong(audioFile *AudioFile) (err error) {
 
 			if buttonName == "no" || buttonName == "" {
 				gomu.pages.RemovePage("confirmation-popup")
+				gomu.popups.pop()
 				return
 			}
 
