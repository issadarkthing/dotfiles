--- a/playlist.go
+++ b/playlist.go
@@ -339,7 +339,6 @@ func (p *Playlist) addToQueue(audioFile *AudioFile) {
 }
 
 // bulk add a playlist to queue
-func (playlist *Playlist) addAllToQueue(root *tview.TreeNode) {
 
 	var childrens []*tview.TreeNode
 
