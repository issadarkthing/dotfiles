--- a/playlist.go
+++ b/playlist.go
@@ -53,6 +53,7 @@ func NewPlaylist() *Playlist {
 	rootDir, err := filepath.Abs(expandTilde(viper.GetString("music_dir")))
 
 	if err != nil {
+		LogError(err)
 	}
 
 	root := tview.NewTreeNode(path.Base(rootDir)).
