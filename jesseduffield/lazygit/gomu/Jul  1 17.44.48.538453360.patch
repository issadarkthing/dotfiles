--- a/playlist.go
+++ b/playlist.go
@@ -93,9 +93,10 @@ func InitPlaylist() *Playlist {
 
 		case 'a':
 
+			name, _ := pages.GetFrontPage()
 			if name != "mkdir-popup" {
 				CreatePlaylistPopup()
 			}
 		case 'D':
 
 			var selectedDir *AudioFile
