--- a/playlist.go
+++ b/playlist.go
@@ -286,12 +286,13 @@ func populate(root *tview.TreeNode, rootPath string) {
 				IsAudioFile: true,
 				Length:      audioLength,
 				Parent:      root,
+			}
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
