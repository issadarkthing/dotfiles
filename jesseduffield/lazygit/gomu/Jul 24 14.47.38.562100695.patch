--- a/playlist.go
+++ b/playlist.go
@@ -114,10 +114,11 @@ func newPlaylist() *Playlist {
 		case ' ':
 			// Disable default key handler
 			return nil
+
 		case 'a':
 
 			name, _ := gomu.pages.GetFrontPage()
 
 			if name != "mkdir-popup" {
 				createPlaylistPopup()
 			}
