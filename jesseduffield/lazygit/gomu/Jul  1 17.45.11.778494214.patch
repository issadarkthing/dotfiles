--- a/playlist.go
+++ b/playlist.go
@@ -280,15 +280,14 @@ func populate(root *tview.TreeNode, rootPath string) {
 				log(err.Error())
 			}
 
-			IsAudioFile: true,
 			Parent:      root,
 		}
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
