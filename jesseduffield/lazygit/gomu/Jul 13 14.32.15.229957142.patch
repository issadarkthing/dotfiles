--- a/playlist.go
+++ b/playlist.go
@@ -352,6 +352,7 @@ func (p *Playlist) FuzzySearch() {
 		return
 	}
 
+	rootPath := p.GetRoot().GetReference().(*AudioFile).Path
 // Refresh the playlist and read the whole root music dir
 func (p *Playlist) Refresh() {
 
