--- a/playlist.go
+++ b/playlist.go
@@ -429,3 +429,4 @@ func (p *Playlist) CreatePlaylist(name string) error {
 	// if the current node is the root
 	// sets the parent to itself
 	if parentNode == nil {
+		parentNode = selectedNode
