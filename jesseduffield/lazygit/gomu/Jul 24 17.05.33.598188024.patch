--- a/player.go
+++ b/player.go
@@ -283,3 +283,4 @@ func getLength(audioPath string) (time.Duration, error) {
 // readable form (0 - 100)
 func volToHuman(volume float64) int {
 	return int(volume*10) + 100
+}
