--- a/player.go
+++ b/player.go
@@ -280,3 +280,4 @@ func getLength(audioPath string) (time.Duration, error) {
 }
 
 // volToHuman converts float64 volume that is used by audio library to human
+// readable form (0 - 100)
