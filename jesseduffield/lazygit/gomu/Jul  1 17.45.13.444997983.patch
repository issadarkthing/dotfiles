--- a/playlist.go
+++ b/playlist.go
@@ -284,12 +284,13 @@ func populate(root *tview.TreeNode, rootPath string) {
 				Name:        file.Name(),
 				Path:        path,
 				IsAudioFile: true,
+				Length:      audioLength,
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
