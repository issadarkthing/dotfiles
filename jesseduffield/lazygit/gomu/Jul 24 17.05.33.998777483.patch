--- a/player.go
+++ b/player.go
@@ -37,12 +37,11 @@ func newPlayer() *Player {
 	// Read initial volume from config
 	var initVol float64 = (viper.GetFloat64("volume") - 50.0) / 10.0
 
-	if initVol > 100 {
 		initVol = 100
 	} else if initVol < 1 {
 		initVol = 0
 	}
 
 	return &Player{volume: initVol}
 }
 
