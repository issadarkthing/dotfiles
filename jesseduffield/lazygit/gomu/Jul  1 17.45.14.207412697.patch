--- a/playlist.go
+++ b/playlist.go
@@ -311,6 +311,7 @@ func populate(root *tview.TreeNode, rootPath string) {
 }
 
 // add to queue and update queue panel
+func (p *Playlist) addToQueue(audioFile *AudioFile) {
 
 	if audioFile.IsAudioFile {
 
