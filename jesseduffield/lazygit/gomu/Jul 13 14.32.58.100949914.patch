--- a/playlist.go
+++ b/playlist.go
@@ -554,6 +554,7 @@ func Ytdl(url string, selPlaylist *tview.TreeNode) (error, chan error) {
 	cmd.Stdout = &stdout
 	cmd.Stderr = &stderr
 
+	e := make(chan error)
 	go func() {
 
 		err := cmd.Run()
