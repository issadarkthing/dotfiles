--- a/player.go
+++ b/player.go
@@ -287,3 +287,4 @@ func volToHuman(volume float64) int {
 
 // absVolume converts human readable form volume (0 - 100) to float64 volume
 // that is used by the audio library
+func absVolume(volume int) float64 {
