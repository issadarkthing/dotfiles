--- a/playlist.go
+++ b/playlist.go
@@ -555,6 +555,7 @@ func Ytdl(url string, selPlaylist *tview.TreeNode) (error, chan error) {
 	cmd.Stderr = &stderr
 
 	e := make(chan error)
+
 	go func() {
 
 		err := cmd.Run()
