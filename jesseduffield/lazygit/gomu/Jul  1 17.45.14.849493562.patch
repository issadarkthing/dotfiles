--- a/playlist.go
+++ b/playlist.go
@@ -390,3 +390,4 @@ func (p *Playlist) AddSongToPlaylist(audioPath string, selPlaylist *tview.TreeNo
 	f, err := os.Open(audioPath)
 
 	if err != nil {
+		return err
