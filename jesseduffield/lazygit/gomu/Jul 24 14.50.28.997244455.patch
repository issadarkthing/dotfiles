--- a/start.go
+++ b/start.go
@@ -165,16 +165,15 @@ func start(application *tview.Application, args Args) {
 				application.Stop()
 			}
 
-					logError(err)
 				}
 
 				if err := viper.WriteConfig(); err != nil {
 					logError(err)
 				}
 
 				application.Stop()
 
 			})
 
 		case ' ':
 			gomu.player.togglePause()
