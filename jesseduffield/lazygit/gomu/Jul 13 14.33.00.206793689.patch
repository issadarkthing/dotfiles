--- a/playlist.go
+++ b/playlist.go
@@ -574,11 +574,12 @@ func Ytdl(url string, selPlaylist *tview.TreeNode) (error, chan error) {
 		if err != nil {
 			appLog(err)
 			e <- err
+		}
 
 		err = gomu.Playlist.AddSongToPlaylist(audioPath, selPlaylist)
 
 		if err != nil {
 			appLog(err)
 		}
 
 		downloadFinishedMessage := fmt.Sprintf("Finished downloading\n%s",
