--- a/playlist.go
+++ b/playlist.go
@@ -185,6 +185,7 @@ func NewPlaylist() *Playlist {
 			if audioFile.IsAudioFile {
 				gomu.Queue.Enqueue(audioFile)
 				currNode.SetExpanded(true)
+			}
 
 		case 'h':
 
