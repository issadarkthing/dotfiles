--- a/player.go
+++ b/player.go
@@ -261,7 +261,6 @@ func (p *Player) toggleLoop() bool {
 
 // Gets the length of the song in the queue
 func getLength(audioPath string) (time.Duration, error) {
-
 	f, err := os.Open(audioPath)
 
 	if err != nil {
