--- a/playlist.go
+++ b/playlist.go
@@ -274,23 +274,22 @@ func populate(root *tview.TreeNode, rootPath string) {
 				continue
 			}
 
-		}
 
 		child := tview.NewTreeNode(file.Name())
 		root.AddChild(child)
 
 		audioFile := &AudioFile{
 			Name:        file.Name(),
 			Path:        path,
 			IsAudioFile: true,
 			Parent:      root,
 		}
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
