--- a/playlist.go
+++ b/playlist.go
@@ -580,6 +580,7 @@ func Ytdl(url string, selPlaylist *tview.TreeNode) (error, chan error) {
 
 		if err != nil {
 			appLog(err)
+			e <- err
 		}
 
 		downloadFinishedMessage := fmt.Sprintf("Finished downloading\n%s",
