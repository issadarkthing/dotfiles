--- a/playlist.go
+++ b/playlist.go
@@ -524,6 +524,7 @@ func Ytdl(url string, selPlaylist *tview.TreeNode) (error, chan error) {
 
 	if err != nil {
 		timedPopup(" Error ", "youtube-dl is not in your $PATH", getPopupTimeout(), 0, 0)
+		return err, nil
 	}
 
 	dir := viper.GetString("music_dir")
