--- a/player.go
+++ b/player.go
@@ -123,6 +123,7 @@ func (p *Player) Run(currSong *AudioFile) {
 	p.position = position()
 	p.IsRunning = true
 
+	gomu.PlayingBar.NewProgress(currSong.Name, int(p.length.Seconds()), 100)
 	gomu.PlayingBar.Run()
 
 	// is used to send progress
