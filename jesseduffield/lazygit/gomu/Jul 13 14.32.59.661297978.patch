--- a/playlist.go
+++ b/playlist.go
@@ -573,12 +573,11 @@ func Ytdl(url string, selPlaylist *tview.TreeNode) (error, chan error) {
 
 		if err != nil {
 			appLog(err)
-		} 
 
 		err = gomu.Playlist.AddSongToPlaylist(audioPath, selPlaylist)
 
 		if err != nil {
 			appLog(err)
 		}
 
 		downloadFinishedMessage := fmt.Sprintf("Finished downloading\n%s",
