--- a/playlist.go
+++ b/playlist.go
@@ -435,3 +435,4 @@ func (p *Playlist) CreatePlaylist(name string) error {
 	audioFile := parentNode.GetReference().(*AudioFile)
 
 	err := os.Mkdir(path.Join(audioFile.Path, name), 555)
+
