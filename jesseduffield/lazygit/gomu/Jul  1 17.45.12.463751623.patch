--- a/playlist.go
+++ b/playlist.go
@@ -280,12 +280,13 @@ func populate(root *tview.TreeNode, rootPath string) {
 				log(err.Error())
 			}
 
+			audioFile := &AudioFile{
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
