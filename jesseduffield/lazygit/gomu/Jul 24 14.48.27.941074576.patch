--- a/popup.go
+++ b/popup.go
@@ -156,6 +156,7 @@ func timedPopup(
 		// this here check if another popup exists and focus that instead of panel
 		// if none continue focus panel
 		topPopup := gomu.popups.peekTop()
+		if topPopup == nil {
 	}()
 }
 
