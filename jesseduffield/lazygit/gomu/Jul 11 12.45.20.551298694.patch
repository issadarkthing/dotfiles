--- a/player.go
+++ b/player.go
@@ -32,22 +32,21 @@ type Player struct {
 	currentSong *AudioFile
 }
 
 func (p *Player) Run() {
 
 	p.isSkipped = make(chan bool, 1)
 	first, err := gomu.Queue.Dequeue()
 	// ensuring the list is updated
 	gomu.App.Draw()
 
 	if err != nil {
 		p.IsRunning = false
 		appLog(err)
 	}
 func (p *Player) Run(currSong *AudioFile) {
 
 	p.isSkipped = make(chan bool, 1)
 
-	f, err := os.Open(first.Path)
 
 	if err != nil {
 		appLog(err)
