--- a/playlist.go
+++ b/playlist.go
@@ -280,16 +280,15 @@ func populate(root *tview.TreeNode, rootPath string) {
 				log(err.Error())
 			}
 
-			Path:        path,
 			IsAudioFile: true,
 			Parent:      root,
 		}
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
