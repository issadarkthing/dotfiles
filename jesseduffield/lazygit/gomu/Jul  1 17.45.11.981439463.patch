--- a/playlist.go
+++ b/playlist.go
@@ -280,14 +280,13 @@ func populate(root *tview.TreeNode, rootPath string) {
 				log(err.Error())
 			}
 
-			Parent:      root,
 		}
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
