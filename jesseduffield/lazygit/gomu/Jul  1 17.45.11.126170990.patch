--- a/playlist.go
+++ b/playlist.go
@@ -280,18 +280,17 @@ func populate(root *tview.TreeNode, rootPath string) {
 				log(err.Error())
 			}
 
-		audioFile := &AudioFile{
 			Name:        file.Name(),
 			Path:        path,
 			IsAudioFile: true,
 			Parent:      root,
 		}
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
