--- a/playlist.go
+++ b/playlist.go
@@ -460,6 +460,7 @@ func (p *Playlist) AddSongToPlaylist(
 
 }
 
+// Gets all audio files walks from music root directory
 func (p *Playlist) GetAudioFiles() []*AudioFile {
 
 	root := p.GetRoot()
