--- a/playlist.go
+++ b/playlist.go
@@ -281,12 +281,13 @@ func populate(root *tview.TreeNode, rootPath string) {
 			}
 
 			audioFile := &AudioFile{
+				Name:        file.Name(),
 
 		child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
