--- a/playlist.go
+++ b/playlist.go
@@ -288,10 +288,11 @@ func populate(root *tview.TreeNode, rootPath string) {
 				Parent:      root,
 			}
 
+			child.SetReference(audioFile)
 
 		if file.IsDir() {
 			audioFile.IsAudioFile = false
 			populate(child, path)
 		}
 
 	}
