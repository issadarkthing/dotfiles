--- a/playlist.go
+++ b/playlist.go
@@ -430,3 +430,4 @@ func (p *Playlist) CreatePlaylist(name string) error {
 	// sets the parent to itself
 	if parentNode == nil {
 		parentNode = selectedNode
+	}
