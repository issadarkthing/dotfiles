--- a/start.go
+++ b/start.go
@@ -165,13 +165,12 @@ func start(application *tview.Application, args Args) {
 				application.Stop()
 			}
 
 
-					logError(err)
 				}
 
 				application.Stop()
 
 			})
 
 		case ' ':
 			gomu.player.togglePause()
