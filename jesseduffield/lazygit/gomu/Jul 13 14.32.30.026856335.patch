--- a/playlist.go
+++ b/playlist.go
@@ -381,7 +381,6 @@ func (p *Playlist) Refresh() {
 	root.Walk(func(node, parent *tview.TreeNode) bool {
 
 		// to preserve previously highlighted node
-		if node.GetReference().(*AudioFile).Name == prevFileName {
 			p.SetHighlight(node)
 			return false
 		}
